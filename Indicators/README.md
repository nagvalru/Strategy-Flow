# Indicators

Use this folder for shared custom indicator source code, build files, and compiled library artifacts when this repository owns them.

Prefer one shared indicator library for reusable project indicators instead of creating one separate library for each small strategy helper.

Before adding a custom indicator, confirm that the strategy cannot be implemented clearly with built-in TSLab blocks and formulas.

